# Core capture dependencies
opencv-python>=4.5      # camera capture + preview window
python-can>=4.0         # SocketCAN listener (receive-only)
PyYAML>=5.4             # config.yaml parsing

# Data handling / post-processing tools
pandas>=1.3             # used by tools/ (sync, inspect, stats)
numpy>=1.20

# Optional but recommended
rich>=12.0              # live terminal dashboard (falls back to plain print if missing)
cantools>=37.0          # optional DBC decoding in tools/sync_dataset.py

# Notes:
# - On NVIDIA Jetson, OpenCV is usually preinstalled with CUDA/GStreamer support.
#   In that case you may SKIP installing opencv-python here to keep the system build.
#   Test first:  python3 -c "import cv2; print(cv2.__version__)"
# - python-can uses the kernel SocketCAN interface; no extra driver needed for can0.
